randomNumber = () => Math.floor(Math.random() * 100);

toUpperFunction = (text) => text.toUpperCase();

firstLetterOf = (word) => word[0];

concatenateStrings = (a, b) => a.concat('', b);

module.exports = { randomNumber, toUpperFunction, firstLetterOf, concatenateStrings };